[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = src
addopts = --strict-markers -v -s --cov=src --cov-report=term-missing
asyncio_default_fixture_loop_scope = function
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
